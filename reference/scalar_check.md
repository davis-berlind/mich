# Scalar value check.

`scalar_check()` throws an error if `x` is not equal to a scalar value.

## Usage

``` r
scalar_check(x)
```

## Arguments

- x:

  A scalar.

## Value

A scalar If no error is thrown during evaluation, then `scalar_check()`
returns the `x` argument.
